// jam.js - 仓库屏蔽配置文件（手动指定需屏蔽的仓库名称）
export const JAM_CONFIG = {
    // 手动添加要屏蔽的仓库名称（支持多个，直接写仓库名即可）
    blockedRepoNames: [
        '25eqsg3f08-stack.github.io', // 主索引仓库（必屏蔽）
        // 可在此处添加其他需要屏蔽的仓库，示例：
        // 'test-repo',
        // 'old-project'
    ]
};


